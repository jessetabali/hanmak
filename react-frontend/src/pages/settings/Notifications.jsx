export default function Notifications() {
  return (
    <div>
      <div className="page-header"><div><h1 className="page-title">Notifications</h1><p className="page-subtitle">Manage email and in-app notification preferences</p></div></div>
      <div className="card" style={{ padding: '1.5rem' }}>
        <p style={{ color: 'var(--text-muted)', fontSize: '0.875rem' }}>Notification preference management — full implementation pending.</p>
      </div>
    </div>
  );
}
